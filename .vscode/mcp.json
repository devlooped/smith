{
	"servers": {
		"latex": {
			"type": "stdio",
			"command": "dotnet",
			"cwd": "${workspaceFolder}${/}mcp",
			"args": [
				"run",
				"latex.cs",
			],
			"env": { 
				// Enables Edit and Continue
				"COMPLUS_ForceENC": "1",
			}
		}
	}
}